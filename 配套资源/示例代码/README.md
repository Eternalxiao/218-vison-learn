# 示例代码

- `maixpy/`：MaixCAM Pro 端的取图、找色块、视觉工具和模型推理。
- `opencv/`：PC 端可保存中间结果的传统视觉处理链。
- `yolo11/`：抽帧、数据划分、训练、验证、ONNX 导出和量化图片准备。
- `通信与gcode/`：字符串、JSON、二进制帧、坐标校验和安全 G-code 日志。

PC 示例依赖见项目根目录 `requirements.txt`。MaixPy 示例需要复制到设备中运行，不能直接使用桌面 Python 环境执行。
