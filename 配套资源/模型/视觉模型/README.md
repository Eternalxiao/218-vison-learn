# 本目录保存了部分视觉模型示例

`.txt` 中记录了模型对应的题目，，使用时候需要注意图像大小的匹配。

`.mud` 中记录模型类型、输入尺寸和类别等描述，`.cvimodel` 是 MaixCAM Pro 执行的模型。两个文件必须放在一起，不能与其他同名或不同版本模型混用。

板端推理入口见 `../示例代码/maixpy/yolo11_infer.py`。同时所有模型文件的输入图像尺寸都是 320\*224，这里不声称开箱即用，实际使用前请完成一次人工验收并记录固件、MaixPy 版本、输入尺寸、类别和推理结果。
